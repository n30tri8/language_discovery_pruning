# config.yaml
workerPoolSpecs:
  machineSpec:
    machineType: n1-standard-16
#    acceleratorType: NVIDIA_TESLA_T4
#    acceleratorCount: 2
  replicaCount: 1
  containerSpec:
    imageUri: us-west1-docker.pkg.dev/{project-id}/{artifact-registry}/pruning
    env:
    - name: HF_TOKEN
      value: <hf_...>
    - name: RUN_ENV
      value: google_cloud
#    command: ["python", "your_script.py"] # Optional: Override container's ENTRYPOINT
#    args: ["--arg1", "value1"] # Optional: Override container's CMD


